# Install only what you actually use:
#   pip install pyserial             -> serial_adapter.py, serial transport
#   pip install flask                -> dnc_web.py
#   pip install wsgidav cheroot      -> dnc_webdav.py
#
# focas.py / focas_probe.py / focas_transfer.py need no pip at all: plain ctypes
# (they only require 32-bit Python + Fanuc's Fwlib32.dll).
# dnc_tftp.py needs nothing external either (socket/struct are builtins).

pyserial
flask
wsgidav
cheroot

# Running the test suite (no hardware needed):
#   pip install pytest && python -m pytest tests/ -q
pytest
